# Progress

Current story: `docs/stories/story-28.md`

Current section: Story 28.5 — Verification and Regression Checks (complete)

Previous tasks (latest completed batch only):
- [x] Add `fileVersions` table to `drizzle/app-schema.ts` (split from `drizzle/schema.ts` to survive `auth:schema` regen)
- [x] Update `drizzle.config.ts` to include both schema files
- [x] Run `pnpm db:generate` to create the migration
- [x] Run `pnpm db:migrate` to apply the migration
- [x] Implement `lib/fs/file-versions.ts` (capture/list/get/delete/rename + dedup + prune-to-5)
- [x] Create `app/actions/file-versions.ts` (auth-gated server actions + rollback)
- [x] Add `readFileContent` to `lib/fs/file-cache.ts` for mutation contexts
- [x] Wire `captureFileVersion` into `saveDocumentAction` + `PUT /api/fs/file`
- [x] Add cleanup hooks (delete/folder-delete/move-rename)
- [x] Add `"versions"` panel type to `stores/layout.ts`
- [x] Add version-preview state to `stores/editor.ts`
- [x] Create `components/version-history/version-history-sidebar.tsx`
- [x] Add read-only preview + banner to `workspace-body.tsx`
- [x] Add History toolbar button to `header.tsx` (desktop + mobile)
- [x] Wire panel switch in `page.tsx` + panel title in `use-right-sidebar-panel.tsx`
- [x] Thread `openVersionHistorySidebar` callback through app-shell → page → vault-workspace → header
- [x] `pnpm lint` passes
- [x] `pnpm build` passes

Next tasks:
- (none — ready for PR)

Notes:
- Branch: `feature/file-version-history`
- New feature: markdown file version history (latest 5 + current) with rollback. Store version snapshots in Turso (SQLite), not S3.
- Architecture decision: Turso for version storage — small text files, SQL ordering/dedup/prune, keeps S3 vault prefix untouched. See `docs/decisions/ADR-file-version-history-storage.md`.
- Schema split: `drizzle/app-schema.ts` holds app tables; `drizzle/schema.ts` is auth-only (regenerated by `auth:schema`). Both combined in `drizzle.config.ts`.
