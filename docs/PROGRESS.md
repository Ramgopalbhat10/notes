# Progress

Current issue: `docs/issues/issue-43.md`

Current section: Issue 43 — @libsql/client Turbopack bundling fix (resolved)

Previous tasks (latest completed batch only):
- [x] Identified root cause: Turbopack mangles `@libsql/client` module name in serverless bundles
- [x] Added `serverExternalPackages: ["@libsql/client"]` to `next.config.ts`
- [x] Cherry-picked fix onto `fix/libsql-server-external-package` branch from latest `main`
- [x] Created issue-43.md and learning entry
- [x] `pnpm lint` passes
- [x] `pnpm build` passes

Next tasks:
- (none — ready for PR merge)

Notes:
- Branch: `fix/libsql-server-external-package`
- Previous feature branch: `feature/file-version-history` (merged via PR #118)
- New feature: markdown file version history (latest 5 + current) with rollback. Store version snapshots in Turso (SQLite), not S3.
- Architecture decision: Turso for version storage — small text files, SQL ordering/dedup/prune, keeps S3 vault prefix untouched. See `docs/decisions/ADR-file-version-history-storage.md`.
- Schema split: `drizzle/app-schema.ts` holds app tables; `drizzle/schema.ts` is auth-only (regenerated by `auth:schema`). Both combined in `drizzle.config.ts`.
